"""Utility environments for harness testing and ad-hoc diagnostics.

These environments do not exercise the rehearse system under test. They exist
to verify harness plumbing (echo) or issue one-off model calls for debugging
(text_probe). Neither is tied to a specific eval or benchmark.
"""
